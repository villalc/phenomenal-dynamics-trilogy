[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sovereign-swarmguard"
version = "0.1.0"
description = "Multi-agent framework for critical infrastructure defense with ZK-SNARK verification and blockchain audit trails"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  {name = "AHI Governance Labs", email = "enterprise@ahigovernance.com"}
]
dependencies = [
  "numpy",
  "cryptography"
]
